Please make only the following edits (no new folders, no extra files):

1) Open client/src/pages/home/Hero.tsx and REPLACE the component with the code below. This removes the bad inline photo, keeps the EV CHARGING pill, adds a small square crest (properly sized), and uses a soft watermark wing behind the title. Keep routes and CTA logic unchanged.

--- REPLACEMENT START (Hero.tsx) ---
import React from "react";
import { Link } from "wouter";

export default function Hero() {
  return (
    <section className="relative overflow-hidden bg-gradient-to-b from-white via-neutral-50 to-neutral-100">
      {/* soft gold glow */}
      <div className="pointer-events-none absolute inset-0">
        <div className="absolute inset-0 bg-[radial-gradient(60%_40%_at_50%_0%,rgba(212,175,55,0.12),transparent_70%)]" />
      </div>

      <div className="relative mx-auto max-w-6xl px-6 py-16 sm:py-20">
        {/* small crest above pill (square image so it won’t slice) */}
        <img
          src="/assets/au-crest-square.png"
          alt="Alchemy United crest"
          className="mx-auto mb-4 h-10 w-10 object-contain"
          width={40}
          height={40}
          onError={(e) => ((e.currentTarget.style.display = 'none'))}
        />

        {/* SEO-friendly category pill */}
        <div className="mx-auto w-fit rounded-full border border-yellow-500/30 bg-white/75 px-4 py-2 text-sm font-semibold text-neutral-700 shadow-sm backdrop-blur">
          <span className="mr-2">⚡</span> EV CHARGING
        </div>

        {/* watermark wings behind title */}
        <img
          src="/assets/wing-logo.png"
          alt=""
          aria-hidden="true"
          className="pointer-events-none absolute left-1/2 top-28 -z-10 w-[min(95vw,780px)] -translate-x-1/2 opacity-15"
        />

        {/* Title */}
        <h1 className="mt-6 text-center font-extrabold leading-tight tracking-tight">
          <span className="block text-5xl sm:text-6xl md:text-7xl text-neutral-900">Alchemy</span>
          <span className="block text-5xl sm:text-6xl md:text-7xl text-[#D4AF37]">United</span>
        </h1>

        {/* Subcopy */}
        <p className="mx-auto mt-6 max-w-3xl text-center text-lg sm:text-xl text-neutral-600">
          Discover the pinnacle of EV charging. Alchemy United connects you to an elite network of gold-standard stations — fast, reliable, and reserved for discerning drivers.
        </p>

        {/* CTAs */}
        <div className="mx-auto mt-8 flex w-full max-w-lg flex-col gap-4 sm:flex-row sm:justify-center">
          <Link href="/early-access" className="w-full sm:w-auto">
            <button className="w-full rounded-2xl bg-[#D4AF37] px-7 py-4 font-semibold text-black shadow-[0_10px_30px_rgba(212,175,55,0.35)] transition-transform duration-200 hover:-translate-y-0.5 active:translate-y-0">
              Get Early Access
            </button>
          </Link>
          <Link href="/host-application" className="w-full sm:w-auto">
            <button className="w-full rounded-2xl border border-[#D4AF37]/50 bg-white px-7 py-4 font-semibold text-neutral-800 shadow-sm transition-all duration-200 hover:shadow-md">
              Become a Host
            </button>
          </Link>
        </div>
      </div>
    </section>
  );
}
--- REPLACEMENT END (Hero.tsx) ---

2) Assets (no new folders):
- Ensure the watermark file exists at: client/public/assets/wing-logo.png
- Add or replace a **square** crest image at: client/public/assets/au-crest-square.png (recommend ~200×200, transparent PNG). If you only have /assets/alchemy-hero.jpg, DO NOT use it in the hero—keep only the crest + watermark above.

3) Do NOT touch routing, forms, or other pages. Don’t create extra files. Just the single component swap + asset check.

After saving, redeploy and verify on mobile:
- No tall/sliced photo above the pill
- “EV CHARGING” pill visible and centered
- “Alchemy United” title with gold/black duo-tone
- Soft wing watermark behind the title